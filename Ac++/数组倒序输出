#include<iostream>
using namespace std;
int main() {
  int a[21], i, j;
  for (i = 0; i < 21; i++) {
    cin >> a[i];
    if (a[i] == 0) {
      break;
    }
  }
  for (j = i - 1; j >= 0; j--) {
    cout << a[j];
    if (j != 0)
      cout << " ";
  }
  return 0;
}
